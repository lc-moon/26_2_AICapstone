# 현황

> **지금 어떤 상태이고 무엇이 확정됐는가.** 상황이 바뀌면 이 문서를 갱신한다.
> 배경·명세는 `프로젝트.md`, 시점별 기록은 `기록/`, 서버 운영은 `운영.md`.
> 갱신: 2026-09-22

---

## 1. 한 줄

**수집 가동 중.** 2026-09-21 23:57 KST부터 인천 CCTV 20대에서 이미지를, 격자 13곳에서 기상 실황을 받고 있다.
PM2.5 라벨과 병합은 아직 붙이지 않았다(3개월 소급되므로 급하지 않다).

---

## 2. 수집 사양

| 항목 | 값 | 이유 |
|---|---|---|
| 소스 | 인천교통정보센터(fitic) **20대** | §4 |
| 주기 | **20분** (매시 00·20·40) | 아래 |
| 야간 | 태양고도 **0° 미만 건너뜀** | 조명 조건이 달라 학습에 쓰지 않음 |
| 해상도 | 대부분 960×540, 일부 1280×720 | 스트림 원본 |
| 품질 | JPEG 95 | 원경의 미세 대비가 신호라 압축에 민감 |
| 저장 | `data/images/{번호}/{날짜}/{HHMM}.jpg` | |
| 기상 | 격자 **13개**, 매시 **:40** | 백필 불가라 야간에도 수집 |
| PM2.5 | **상시 수집 안 함** | 3개월 소급 가능 → 배치로 |
| 용량 | 4개월 약 18GB | 30GB 디스크. GCS 동기화 필요 |
| 알림 | Discord — 장애 즉시 + 매일 00시 요약 | |

### 주기를 20분으로 정한 이유

PM2.5 라벨이 1시간 단위라 시간당 6장(10분 간격)은 독립 샘플이 아니다.
그렇다고 1시간에 1장이면 **한 번 실패할 때 그 시간이 영구 손실**이고, 차량 가림·시각 정렬에도 대응 못 한다.

> **수집 주기 ≠ 학습 샘플링 주기**
> 수집은 시간당 3장(결측·가림 보험), 학습은 시간당 1샘플로 집계하거나 대표 1장 선택.
> **train/test는 무작위가 아니라 날짜 기준으로 분할할 것** — 근접 프레임이 양쪽에 걸리면 성능이 부풀려진다.

### 프로세스 구성 — 상시 1개 + 배치 1개

백필 가능 여부로 갈린다. **지나가면 사라지는 것만 상시로 받는다.**

```
[상시] collect_cctv.py   이미지(20분) + 기상(매시 :40)     ← 둘 다 백필 불가
[배치] build_labels.py   PM2.5 백필 + 병합 → labels.csv    ← 3개월 안에 언제든 (미구현)
```

기상을 별도 프로세스로 분리하지 않은 이유: **기상값은 이미지가 있어야 의미가 있다.**
이미지 수집기가 죽은 시간대의 기상값은 쓸 데가 없으므로 분리해도 지켜지는 게 없고,
최대 리스크가 "조용히 멈추는 것"인 만큼 감시 지점을 하나로 두는 편이 낫다.
기상 API 실패는 `try/except`로 격리해 이미지 캡처를 막지 않는다.

---

## 3. 검증된 사실 (재확인 불필요)

**데이터 소스**

1. **fitic 카메라 목록을 API로 받을 수 있다**
   ```
   POST https://www.fitic.go.kr/gis/selectListData.do
   body: type=cctv&keyword=
   ```
   - 응답이 **이중 인코딩**(문자열 안에 JSON)이라 두 번 파싱해야 함
   - 필드: `CCTV_MNGM_NMBR`(번호) `CCTV_NM`(이름) `X_CRDN`(경도) `Y_CRDN`(위도) `HTTPADDR`(스트림)
   - 인천 전역 237대. `HTTPADDR`의 `http://61.40.94.13:1935` → `https://cctv.fitic.go.kr`로 치환
   - 공식 오픈 API가 아니라 지도 페이지 내부 엔드포인트이므로 **목록 조회는 하루 1회 수준으로만**

2. **실제 스트림 해상도** — 960×540이 150대, 1280×720이 60대, 640×360 이하 18대.
   브라우저 플레이어로 보면 커 보이지만 업스케일이다

3. **클라우드 IP에서 접속 가능** — 해외 IP·데이터센터 IP 차단 없음, Referer 검사 없음

**라벨 소스**

4. **기상청 초단기실황은 백필 불가 — 최근 1일치뿐.** 1일 전부터 `"최근 1일 간의 자료만 제공합니다"`
   - 자료는 매시 정시 기준, 게시는 매시 40분쯤
   - 격자 변환(Lambert)은 서울 종로(60,127)·인천 미추홀(54,124)·제주(53,38)로 검증 완료

5. **에어코리아 PM2.5는 3개월 소급 가능.** 단 **약관 제5조상 3개월이 상한**이며
   그 이상은 국립환경과학원 대기환경월보의 확정 데이터를 써야 한다 → **월 1회 백필은 필수 운영 규칙**

6. **에어코리아 생성주기 매시 15분 내외.** `dataTime=13:00` 자료는 13:15쯤 게시된다
   → 라벨 병합은 수집과 분리된 후처리여야 한다

7. **에어코리아 측정소 목록은 위경도를 준다** — TM 변환 불필요.
   `getMsrstnList`의 `dmX`=위도, `dmY`=경도. `mangName`으로 도시대기/도로변대기/항만/교외 구분

**환경**

8. **이미지는 소급 불가** — 과거 스트림을 제공하는 곳이 없다. 수집 가동률이 곧 데이터셋 크기
9. **Ubuntu 22.04는 사용 불가** — Python 3.10이라 `tomllib`(3.11+)이 없다. 24.04 사용

---

## 4. 카메라와 측정소

`Tools/screen_fitic.py`로 237대를 자동 채점 후 육안 검수해 **20대**를 확정했다.
목록은 `Collector/cameras.toml`, 선정 과정은 [기록/2026-09-21](기록/2026-09-21_카메라선정과배포.md).

**판정 기준** — 하늘 면적 5% 이상 · 하늘 가로 균일도(p85−p15) 15 이하 · 포화율 · 원경 최저 대비(음수는 실격) · 해상도

**측정소 매칭** — 최근접 **도시대기** 측정소 13곳, 하루 312건(한도 500). 대부분 1~3km, 최장 4.0km.

도로변대기를 쓰지 않는 이유: 도로 옆이라 차량 배기로 농도가 국지적으로 튄다.
우리 이미지는 **수 km 시선 경로의 공기를 적분한 값**이라 성격이 정반대다.

**한계로 남겨둘 것**: 이미지는 카메라 지점이 아니라 시선 경로 전체의 공기를 보는데 라벨은 측정소 한 점의 값이다.
인천은 측정소가 촘촘해 차이가 크지 않을 것으로 보지만, 특정 카메라만 오차가 클 때 원인 후보로 둘 것.

**주의**: 원경 타겟은 **건물이 안전하다.** 산림은 9~11월 단풍·낙엽으로 반사율이 바뀌어 수집 기간과 겹친다.

```
python Tools/screen_fitic.py              전체 촬영 후 채점 (약 6분)
python Tools/screen_fitic.py --rescore    저장된 프레임 재채점 (약 5초)
python Tools/screen_fitic.py --ptz-check  화각 변경 검출 (시간 간격이 클수록 정확)
```

---

## 5. 구조적 리스크 — 고농도 데이터 부재 (미해결)

- 숭의 측정소 최근 3개월: 2,148시간 중 **'나쁨' 이상 38시간(1.8%), '매우나쁨' 0시간**
- 2026년 9월만 보면 373시간 중 **'나쁨' 0건**
- 모델 마감까지 모이는 데이터는 사실상 **0~35㎍/㎥ 구간뿐**
- **인천 안에서 카메라를 분산해도 해결되지 않는다.** 미세먼지 사건은 수백 km 규모라 인천 전역이 같이 움직인다

→ 해법은 둘. **외부 데이터셋 조달**, 또는 **타 시도(강원 영동·서해안·내륙 분지) 카메라 확보**.
타 시도로 가려면 fitic으로는 불가능하고 ITS 전국 API가 필요하다.

### 데이터 규모를 읽는 법

10/25까지 33일 수집한다고 하면:

| 세는 방법 | 수 |
|---|---|
| 이미지 장수 | 약 20,000장 |
| 시간당 1샘플로 집계 | 약 6,900 (카메라-시간) |
| 독립적인 라벨 시점 | 약 350시간 |
| **실질 독립 표본** | **약 33일** |

PM2.5는 시간 단위로 강하게 자기상관된다. "2만 장"은 과대평가이며 실질은 **33일치 대기 상황**이다.

---

## 6. 미확인 — `pm25Value`가 1시간 평균인가 순간값인가

- 공공데이터포털 명세와 기술문서 8종 모두 **"농도"로만 적고 평균 구간을 밝히지 않는다**
- 측정소가 하나뿐인 옹진군으로 교차 확인했으나, 그 경우 "시간축 평균"과 "측정소 간 공간 평균"이
  같은 답을 내므로 **결정적이지 않았다**
- 정황은 1시간 평균 쪽(일평균과 대구를 이룸). 확실히 하려면 한국환경공단 **032-590-3516** 문의
- **영향 범위**: 라벨 병합 규칙만 달라진다. 평균이면 직전 1시간의 3장을 평균, 순간값이면 정시에 가장 가까운 1장.
  어느 쪽이든 20분 수집이면 프레임이 확보되므로 **수집 설계에는 영향 없음**

---

## 7. 다음 작업

```
1. GCS 동기화 + 로컬 정리          ← 18GB가 30GB 디스크에 쌓인다. 차면 조용히 실패함
2. 외부 고농도 데이터셋 조달        ← §5. 구하는 데 시간이 걸릴 수 있어 일찍 시작할 것
3. build_labels.py — PM2.5 백필 + 병합
4. 전처리 — 필터링(강수·고습도·결측), ROI 크롭(카메라 20대 개별 지정)
5. 모델 — 이미지 통계 베이스라인 → CNN 회귀 → 기상 보조변수 결합
6. 3D 시각화(CesiumJS) + 전시 데모
7. 전국 확장 — ITS API (인천 밖은 fitic으로 불가)
```

**수집이 끝나길 기다리지 말 것.** 2주치만 있어도 베이스라인을 돌릴 수 있고,
거기서 *"이미지 통계가 PM2.5와 실제로 상관이 있는가"* 라는 가설 자체가 검증된다.
상관이 안 나오면 방향을 틀 시간이 필요하다.

---

## 8. 아직 확인 안 된 것

- [ ] 에어코리아 `pm25Value`의 평균 구간 (§6)
- [ ] **ITS 인증키 소재** — `프로젝트.md`에는 "발급 완료"인데 `.env`에 없다
- [ ] ITS `cctvurl` 토큰 만료 주기 (캐시→실패시갱신 설계면 실측 전에도 구현 가능)
- [ ] 측정소 공간 대표성의 공식 근거 (현재는 1~4km면 무난하다는 경험적 판단)
- [ ] 느리게 도는 PTZ 카메라 — `--ptz-check`를 시간 간격을 벌려 재실행할 것

---

## 9. 출처 표기 의무

결과보고서·발표자료에 반영해야 한다.

- CCTV 영상: **인천교통정보센터**
- 대기오염 정보: **한국환경공단 에어코리아**, 공공누리 제3유형(출처표시 + 변경금지)
- 에어코리아 자료는 **"인증을 받지 않은 실시간자료"**이므로 값이 다를 수 있음을 명시 (이용약관 제8조)
