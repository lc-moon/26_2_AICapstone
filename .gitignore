# GitHub에 올리지 않을 파일 목록

# 인증키 (절대 올리지 않음)
.env

# 생성물 전부 — 수집 이미지, 로그, 스크리닝 산출물, 분석 결과
# 용량이 계속 늘어나고 사람·차량 번호판이 찍힐 수 있으며, 스크립트로 다시 만들 수 있음
data/

# 수집기가 만드는 캐시 (목록 API로 언제든 다시 받음)
Collector/url_cache.json

# 역할이 끝난 자료 보관함 (UTIC 원본, 폐기 결정된 초기 수집분)
_archive/

# Python 자동 캐시
__pycache__/
